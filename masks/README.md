# masks

Maski (czarno-białe, biały = obszar do edycji) używane w testach workflow.
Nazwa pliku = nazwa w katalogu `input` ComfyUI, do której workflow odwołuje się przez `LoadImageMask`.

Powiązane: obrazy źródłowe → `../test-images/`, wyniki → `../results/`.
