{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the RasterRelay Launcher window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
